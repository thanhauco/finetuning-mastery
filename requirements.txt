# Core deep learning stack (install torch separately first — see SETUP.md)
transformers>=4.48.0
datasets>=3.2.0
accelerate>=1.2.0
peft>=0.14.0
trl>=0.13.0
evaluate>=0.4.3
sentencepiece>=0.2.0
safetensors>=0.4.5

# Quantization (QLoRA) — NVIDIA GPU required
bitsandbytes>=0.45.0 ; platform_system != "Darwin"

# Metrics / utilities
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.67.0

# Optional: experiment tracking (uncomment to use)
# wandb>=0.19.0

# Optional: serving (Module 09) — Linux/WSL with GPU
# vllm>=0.6.6

# Optional: distributed (Module 10)
# deepspeed>=0.16.0

# Optional: standardized eval harness (Module 08)
# lm-eval>=0.4.5
